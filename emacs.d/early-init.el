;;; early-init.el --- default parameters for frame and package initialization
;;; Commentary:
;;; Code:

(custom-set-variables
 '(default-frame-alist '((top . 50)
                         (left . 20)
                         (width . 80)
                         (height . 44)
                         (vertical-scroll-bars . nil)
                         (horizontal-scroll-bars . nil)
                         (tool-bar-lines . 0))))

(provide 'early-init)
;;; early-init.el ends here
