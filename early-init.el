;;; early-init.el --- before init.el
;;; Commentary:
;;; Code:

;; window size
;; (setq default-frame-alist
;;       '((height . 54)
;; 	(width . 180)))

(provide 'early-init)
;;; early-init.el ends here
